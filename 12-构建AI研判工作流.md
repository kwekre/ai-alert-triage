# 12 · 构建 AI 研判工作流

前 11 章是“单条怎么研判”，本章把它**工程化**：让 AI 成为 SOC 管线的一环，人只在关键节点拍板。

## 12.1 总体架构（人机协同闭环）

```
采集层 ─┐
EDR/SIEM/Zeek/WAF/邮件网关/云日志
        │
归一化层 ─→ 统一成 ECS/OCSF 字段（AI 稳定解析的前提）
        │
研判队列 ─→ 按严重度/资产重要度 排序
        │
┌───────┴───────────────────────────────┐
│  AI 研判引擎                            │
│   · 提示词模板（第05章）                │
│   · RAG：接 RUNBOOK/历史案件/资产库/情报 │
│   · 工具调用：查情报/查 Splunk/查资产    │
└───────┬───────────────────────────────┘
        │ 输出：verdict + 置信度 + 证据 + 下一步
人工确认层 ─→ 采纳 / 驳回 / 修正（人在环路）
        │
闭环/SOAR ─→ 低风险自动阻断IP/隔离主机（需审批）
        │
反馈 ──→ 误报/修正回灌规则与知识库
```

## 12.2 归一化：用 ECS/OCSF 让 AI 不乱

不同设备字段名五花八门（`src_ip`/`source.address`/`id.orig_h`）。先统一成标准 schema（ECS 或 OCSF），AI 只需学一套：
```json
{"ecs": {"source": {"ip":"10.0.0.5","port":51123},
         "destination":{"ip":"10.0.1.9","port":443},
         "event":{"action":"alert","kind":"alert"},
         "rule":{"name":"ET SQLi"}}}
```
好处：提示词模板一次写好，换数据源不用改。

## 12.3 RAG：让 AI “引用内部知识”

纯 LLM 不知道你家的 RUNBOOK、资产分级、历史案件。用检索增强（RAG）把相关文档塞进上下文：
- **RUNBOOK**：`收到 SQLi 告警 → 先查 web 日志会话 → 再查 DB 审计`
- **资产库**：`10.0.1.9 = 对外 Web，重要度 High`
- **历史案件**：`上月同类 → 最终确认真，处置 X`
- **威胁情报**：域名/IP 命中记录
AI 输出时可标注“依据 RUNBOOK-#12 / 资产库”，可审计、可溯源。

## 12.4 工具调用（Function Calling）：让 AI 去查，而不是编

给 AI 函数：`query_siem(sql)`、`lookup_ioc(value)`、`get_asset(ip)`、`check_ti(domain)`。
```json
{"name":"lookup_ioc","arguments":{"value":"suspect.test"}}
```
执行层安全调用（只读、限权），把结果回填给 AI 继续研判。
**约束**：函数默认只读；任何“写/阻断”类调用必须人工批准（第 05 章铁律）。

## 12.5 人在环路（Human-in-the-loop）

- AI 输出一律带 `confidence`；低于阈值（如 <0.7）自动转人工。
- 分析师三动作：采纳 / 驳回 / 修正。修正回流训练与规则。
- 关键处置（隔离/封禁）即使 AI 高置信，也走“建议→人点确认”两步。

## 12.6 SOAR 闭环（谨慎启用）

只对**低风险 + 高置信 + 可逆**动作自动化：
- 自动加 IP 到观察名单（非封禁）
- 自动隔离已确认中毒主机（需审批开关）
- 自动发工单/通知
**绝不**自动删数据、自动封核心业务 IP。

## 12.7 编排伪代码（思路）

```python
def triage(alert):
    norm = normalize(alert)                 # ECS
    ctx = rag.retrieve(norm)                # RUNBOOK+资产+历史
    ai_out = llm.judge(norm, ctx, TEMPLATE) # 第05章 JSON
    if ai_out.confidence < 0.7 or ai_out.verdict == "needs_more_data":
        return queue_for_human(ai_out)      # 转人工
    suggest = soar.plan(ai_out)             # 生成处置建议
    return human_approve(suggest)           # 人确认才执行
```

## 12.8 落地顺序（别一上来就全自动）

```
L1 人工 + AI 草稿（AI 只填研判笔记初稿）
L2 AI 聚类降噪（第07章 top-K）
L3 AI 给建议，人确认（本架构）
L4 低风险动作 SOAR 半自动（审批开关）
L5 高置信自动闭环（审慎，仅可逆动作）
```

## 12.9 本章 checklist

- [ ] 能画出“采集→归一化→AI→人确认→闭环”架构
- [ ] 知道 ECS/OCSF 归一化的价值
- [ ] 理解 RAG/工具调用让人机协同可溯源
- [ ] 记住：写/阻断类动作永远人确认

下一章：13 · 私有化与本地部署（数据不出域）。
