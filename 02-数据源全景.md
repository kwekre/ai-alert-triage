# 02 · 数据源全景：从 PCAP 到云日志

研判质量上限 = 你手头数据的覆盖度。先建立“地图”：12 类常见数据源，以及每类**最该看的字段**。

## 2.1 网络层（流量是很多攻击的唯一真相源）

| 数据源 | 形态 | 看什么字段 | 擅长发现 |
|--------|------|------------|----------|
| **PCAP** | 原始包 | 五元组、协议、payload | 一切（但需要你会看） |
| **NetFlow / IPFIX** | 流汇总 | src/dst IP、port、bytes、pkts、duration | 扫描、横向、大流量外泄 |
| **Zeek (conn/dns/http/files)** | 会话日志 | `conn.log` 速率、`dns.log` 查询、`http.log` URI/UA/status | Web 攻击、DNS 隧道、C2 |
| **Suricata / Eve** | IDS 告警+流 | `alert.signature`、`flow_id`、`http.uri` | 已知攻击特征 |

## 2.2 应用与边界层

| 数据源 | 看什么 | 擅长 |
|--------|--------|------|
| **WAF 访问日志** | uri、args、rule_id、action(block/pass)、client_ip | Web 攻击探测（被拦的也很有价值） |
| **反向代理 / LB** | X-Forwarded-For、真实 client、status | 真实源 IP（WAF 后常需解 XFF） |
| **Web 服务器日志** | method、path、status、UA、referer | 踩点、目录爆破、异常 UA |
| **邮件网关** | 发件域、SPF/DKIM/DMARC、URL、附件哈希 | 钓鱼 |

## 2.3 主机与身份层

| 数据源 | 看什么 | 擅长 |
|--------|--------|------|
| **EDR** | 进程树、命令行、父子关系、文件写、注册表 | 落地、提权、持久化、离地攻击 |
| **Windows 安全日志** | 4625(失败登录)/4688(进程创建)/4672(特权)/4768-4769(Kerberos) | 爆破、PTH、黄金票据 |
| **Linux auditd** | execve、syscall | 命令执行、提权 |
| **IAM / SSO** | 登录成功/失败、MFA、异常地点 | 账号被盗、横向 |

## 2.4 云与 SaaS 层（越来越重要）

| 数据源 | 看什么 | 擅长 |
|--------|--------|------|
| **CloudTrail** | eventName、userIdentity、sourceIP、errorCode | 控制台异动、AK 泄露、提权 |
| **VPC Flow Logs** | 流、action(ACCEPT/REJECT) | 云内横向、暴露面 |
| **S3 访问日志** | bucket、key、requester | 数据外泄 |
| **K8s audit** | verb、object、user | 容器逃逸、提权 |

## 2.5 怎么把数据源变成“研判能力”

记住一个原则：**流量证“发生了什么动作”，端点证“执行了什么”，身份证“谁操作的”，云证“边界外发生了什么”。**
单独一块都说不全，研判就是**交叉印证**。

例子：一条 Suricata `Web 攻击` 告警，只说明“有人试了”。要闭环，需要：
- WAF/Web 日志看**响应是不是 200 + 异常内容**（请求+响应）
- EDR 看**那台 Web 服务器之后有没有新进程**（落地）
- 身份日志看**有没有新会话/提权**（横向起点）

## 2.6 给 AI 的数据源提示

当你让 AI 帮你研判，最好先告诉它**有哪些数据源可用**，否则它会瞎猜：
> “我有 Zeek conn/dns/http 日志、Suricata 告警、EDR 进程树。请基于这些给研判步骤，不要假设没有的数据。”

## 2.7 本章 checklist

- [ ] 能列出网络/应用/主机/身份/云 五层各 2+ 数据源
- [ ] 知道“流量/端点/身份/云”分别证什么
- [ ] 理解“研判 = 交叉印证”

下一章：03 · 流量研判工具链——把上面“网络层”落到真命令。
