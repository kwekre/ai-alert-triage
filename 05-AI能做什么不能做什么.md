# 05 · AI 能做什么、不能做什么

用 AI 研判最大的坑，是把它当“算命”：丢一堆日志，问“有没有被黑”。本章划清边界，给你**提示词四要素**。

## 5.1 能力边界（先记这张表）

| AI 擅长 ✅ | AI 不擅长 ❌ |
|------------|--------------|
| 把杂乱日志**归纳/翻译**成人话 | 凭空判定“一定被黑了”（无证据下结论） |
| 从文本里**抽取结构化字段**（IOC、时间、主机） | 访问实时数据/查生产系统（它没接口） |
| 按描述**写查询**（Splunk/KQL/tshark 过滤） | 保守秘密（默认会把内容发去云端） |
| 给**可验证的假设**和下一步动作 | 解释“为什么这样判”（常编造来源） |
| 对大量告警**聚类/降噪** | 替你担责任（封 IP/隔离必须人确认） |
| 把口语需求转成 ATT&CK/处置建议 | 处理超大原始包（超上下文会截断） |

一句话：**AI 是“会看日志的实习生”，不是“值班经理”。**

## 5.2 三大风险

1. **幻觉（Hallucination）**：它会编出“我看到响应里返回了管理员密码”这种没发生的事。对策：**要求它只基于你给的文本，逐条引用原文**。
2. **数据出域**：把客户日志贴进公网模型 = 可能的合规事故。对策：脱敏（第 06 章）+ 私有化（第 13 章）。
3. **过度自信**：模型常以肯定语气说错话。对策：让它在结论里给**置信度 + 证据位置**，你不确认就不采纳。

## 5.3 提示词四要素（所有章节通用）

结构化的提示 = **角色 + 任务 + 上下文 + 输出格式/约束**。缺一个就废一半。

```
角色(Role):   你是资深 SOC 分析师，只基于给定证据下结论。
任务(Task):    研判下面这条告警是否为真实攻击，并给下一步。
上下文(Context):<粘贴脱敏后的日志/告警 JSON/流量抽取>
输出(Format):  严格按 JSON 返回：
  {
    "verdict": "true|false|needs_more_data",
    "confidence": 0.0-1.0,
    "evidence": ["直接引用原文的 1-3 处"],
    "missing":  ["还差什么证据才算闭环"],
    "next_steps": ["具体可执行的查询/动作"],
    "attack_mapping": "ATT&CK 战术.技术 或 null"
  }
约束(Constraints): 不得编造原文没有的信息；不确定就写 needs_more_data。
```

## 5.4 一个反例 vs 正例

**反例（别这样问）：**
> “帮我看看这段流量有没有攻击？”（没给数据、没给格式、没约束 → 必翻车）

**正例（照 5.3 模板）：**
> 角色…任务…上下文（http.log 5 行）…输出 JSON…约束：只基于给定文本。

## 5.5 实战：让 AI 把一条 Suricata 告警压成研判草稿

输入（脱敏后）：
```json
{"signature":"ET SQLi","src_ip":"10.0.0.5","dst_ip":"10.0.1.9",
 "uri":"/item?id=1+UNION+SELECT+password+FROM+users--","resp_code":500}
```
按 5.3 模板问，理想返回：
```json
{"verdict":"needs_more_data","confidence":0.6,
 "evidence":["uri 含 UNION SELECT","resp_code=500(可能触发报错)"],
 "missing":["响应体是否真的回显了数据？","该账号后续是否有数据导出？"],
 "next_steps":["查 web 日志该会话完整请求","查 DB 审计是否被执行"],
 "attack_mapping":"TA0009 Collection:T1213"}
```
注意它**没有**直接说“已入侵”——因为证据还没闭合（铁律一）。这正是好 AI 用法。

## 5.6 什么时候必须人来做

- 任何**改变系统状态**的动作（封禁、隔离、删除）。
- **定性为 True** 且影响 Critical 资产时。
- 证据矛盾、需要业务知识判断“这算正常吗”时。

## 5.7 本章 checklist

- [ ] 能背能力边界表（擅长/不擅长）
- [ ] 知道三大风险（幻觉/出域/过度自信）和对策
- [ ] 能写“角色+任务+上下文+格式+约束”的提示词
- [ ] 记住：AI=实习生，不是值班经理

下一章：06 · 喂给 AI 的正确姿势——脱敏、分块、字段抽取模板。
