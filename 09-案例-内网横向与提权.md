# 09 · 案例：内网横向与提权流量研判

外网突破只是开始，真正危险在内网横向。本章看**流量 + 主机日志**怎么抓横向。

## 9.1 SMB 横向（T1021.002）

**特征**：内网某主机对大量其他主机的 **445** 发连接；伴随 **失败登录（4625）** 或 `admin$`/`C$/IPC$` 共享访问。
**查**：
```bash
# Zeek conn：谁在横扫 445
zeek-cut id.orig_h id.resp_h id.resp_p < logs/conn.log | awk '$3==445' | sort | uniq -c
# Windows 安全日志：失败登录（暴力/PTH 迹象）
wevtutil qe Security /q:"*[System[(EventID=4625)]]" /c:50 /rd:true
```
**定性**：单源对多目的 445 + 失败登录 → **真（High）** 横向尝试；若后续出现成功登录→升 Critical。

## 9.2 RDP 异常（T1021.001）

**特征**：**3389** 来自非常规段（如跳板/外网穿透）、非工作时间、多次失败。
**查**：`conn.log` 看 `id.resp_p==3389` 的 `id.orig_h`；Windows 4625/4778（RDP 会话）。
**定性**：来源异常 + 爆破 → High。

## 9.3 PsExec / WMI 远程执行（T1021.002 / T1047）

**特征**：命令行出现 `psexec`/`wmic /node:`/`winrm`；SMB 上建服务（`svcctl`）。
**查（EDR/4688 进程创建）**：
```
新进程: C:\Windows\PSEXESVC.exe 父: services.exe  来自 INT_10_0_0_5
```
**定性**：非管理员机发起远程执行 → **真（High/Critical）**。

## 9.4 票据攻击（PTH / 黄金票据，T1550 / T1558）

**特征**：Kerberos 异常——`4769` 服务票据请求异常（含 `krbtgt`、加密类型 0x17 老 RC4）、`4768` 异常、登录类型 3 但无对应交互。
**查**：
```bash
wevtutil qe Security /q:"*[System[(EventID=4769)]]" /c:50
# 关注 TicketEncryptionType=0x17(DES) 或请求 krbtgt 票据
```
**定性**：RC4 加密票据 + krbtgt 请求 → **高度疑似黄金票据（Critical）**。

## 9.5 隧道（把横向藏进 DNS/ICMP/HTTP，接第 10 章）

攻击者常用 SOCKS/ICP 把流量封装，逃避明文检测。看：
- 大量 DNS 查询（隧道）→ 第 10 章
- 异常大 ICMP → 第 10 章
- HTTP 长连接规律小包 → C2 beacon

## 9.6 综合研判笔记（模板实战）
```
来源告警: Zeek conn 多目的 445 + Win 4625 爆破
资产: INT_10_0_0_5 是普通办公机（不该发 SMB 到全网）
时间窗: 03:10-03:25
定性: 真（High）—— 办公机对内网横扫+爆破
IOC: src INT_10_0_0_5；targets INT_10_0_1_x 等 30 台
ATT&CK: TA0008 Lateral Movement (T1021.002/T1110)
处置: 1)隔离该机 2)重置相关账号 3)查是否拿到域管 4)分段加固
置信度: AI 0.85 / 分析师确认
```

## 9.7 缓解要点

- 网络分段：办公/服务器/域控隔离，445/3389 不跨段。
- 最小权限：普通机无理由 SMB 到全网。
- 全量日志：4624/4625/4672/4768/4769 必开。
- 特权账号保护：域管不可登录普通机。

## 9.8 本章 checklist

- [ ] 能认 SMB/RDP/PsExec/WMI/Kerberos 各自信号
- [ ] 会用 Zeek conn + Windows 安全日志抓横向
- [ ] 知道 4769 RC4/krbtgt = 黄金票据嫌疑
- [ ] 记得“普通机发 SMB 到全网”本身就是高危

下一章：10 · 案例：C2 与外联研判（DNS 隧道 / JA3 / 域前置）。
