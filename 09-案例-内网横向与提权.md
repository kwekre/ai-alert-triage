# 09 · 实战：内网横向与提权（还那个 INT_10_0_0_5）

第 8 章我们跟到 INT_10_0_0_5 在 Web 上打穿了边界。现在看它**进了内网之后**干了什么——横向，才是最危险的一段。

---

## 9.0 聚类已经告诉我们"这台机不对劲"

回看 `triage_cluster.py` 的输出：INT_10_0_0_5 的 17 条告警里，**除了 Web 手法，还挂着 `SMB横向` 和 `RDP爆破`**。单台办公机同时有"对外 Web 攻击"+"对内网 445/3389"——这就是典型 **边界被突破 → 内网横扫** 的信号。

---

## 9.1 时间线：02:10 它在打内网

从 `sample_alerts.jsonl` 抽 INT_10_0_0_5 的非 HTTP 告警：

```
02:10:11  TCP  INT_10_0_0_5:51123 -> INT_10_0_1_9:445    SMB 连接
02:10:12  TCP  INT_10_0_0_5:51124 -> INT_10_0_1_10:445   SMB 连接
02:10:13  TCP  INT_10_0_0_5:51125 -> INT_10_0_1_11:445   SMB 连接
02:10:30  TCP  INT_10_0_0_5:51126 -> INT_10_0_0_9:3389   RDP，win_event=4625(登录失败 x3)
```

三件事叠加：
1. **对多台内网主机扫 445**（SMB）→ 横向探测/爆破；
2. **打 3389（RDP）且登录失败 4625** → 凭据爆破或撞库；
3. 来源是**办公机**，不是运维跳板——它不该有理由 SMB 到全网。

### 用 Zeek + Windows 日志抓（真实命令）

```bash
# Zeek conn：谁在横扫 445
zeek-cut id.orig_h id.resp_h id.resp_p < logs/conn.log | awk '$3==445' | sort | uniq -c

# Windows 安全日志：失败登录（暴力/PTH 迹象）
wevtutil qe Security /q:"*[System[(EventID=4625)]]" /c:50 /rd:true
```

若 `id.orig_h` 是台普通办公机、`id.resp_p==445` 且目标有几十个 → 基本坐实横向。

---

## 9.2 让 AI 出研判——再翻一次

> **AI 草稿**：INT_10_0_0_5 对 3 台主机 445 + 对 1 台 3389 失败登录，判定 **True / High 横向尝试**；建议封禁该 IP、重置相关账号。

> 🔴 **翻车现场 #1（漏了资产上下文）**：AI 没说"**为什么** High"。关键是资产归属——INT_10_0_0_5 是**办公机**，正常它连一台 445 都不该有。没有"资产不该这样"这条知识，AI 只会给个泛泛的"High"。**人的动作**：查 CMDB，确认它是普通 PC，结论立刻升到"异常且高危"，而不是"可能的扫描噪音"。

> 🔴 **翻车现场 #2（把失败当无害）**：AI 见 4625 都是"失败"，差点写"未成功，降权观察"。但**失败登录 ×3 在 02:10:30，紧接着 02:13 就出现了 SQLi 成功拖库**——说明这波爆破虽然在这台 3389 上没成，攻击者已经用别的路子进来了。**人的动作**：横向失败 ≠ 入侵失败，要和后继成功事件拼时间线。

> 🔴 **翻车现场 #3（建议越界）**：同 8.2，"封禁 IP"是处置，AI 应给"建议封禁 + 理由"，由人执行。

---

## 9.3 顺手认全横向家族（Detection 清单）

| 技术 | 信号 | ATT&CK |
|------|------|--------|
| SMB 横扫/爆破 | 单源多目的 445 + 4625 | T1021.002 / T1110 |
| RDP 异常 | 3389 来自非常规段/非工作时间/多次失败 | T1021.001 |
| PsExec / WMI | `psexec`/`wmic /node:`/`winrm`；SMB 建服务 `svcctl` | T1021.002 / T1047 |
| 票据攻击(PTH/黄金票据) | `4769` RC4(0x17) 加密 + 请求 `krbtgt`；登录类型 3 无交互 | T1550 / T1558 |
| 隧道藏横向 | 大量 DNS/ICMP/HTTP 规律小包（接第 10 章） | T1095 / T1571 |

```bash
# 黄金票据嫌疑：RC4 加密的服务票据
wevtutil qe Security /q:"*[System[(EventID=4769)]]" /c:50 | findstr "0x17 krbtgt"
```

---

## 9.4 红队视角：他怎么藏横向？

- **Living-off-the-land**：不落地 exe，用 `psexec`/`wmic`/`winrm` 这些系统自带工具——你的 EDR 要看**进程父子关系**（如 `PSEXESVC.exe` 父是 `services.exe`、来源却是办公机），而不是看有没有新文件。
- **慢速爆破**：把 4625 拉长到每小时几次，绕过"X 次/分钟"阈值。
- **用合法票据**：PTH 重用哈希，不碰密码，4625 根本不触发——得看 Kerberos 4768/4769 的加密类型异常。
- **隧道封装**：横向流量塞进 DNS/HTTPS，明文检测看不到（下一章）。

---

## 9.5 处置与缓解

- **定性**：True / High（办公机对内网横扫+爆破；结合第 8 章已确认边界失陷，整体升 Critical）。
- **IOC**：源 `INT_10_0_0_5`；目标段 `INT_10_0_1_x`、`INT_10_0_0_9`；事件 `4625`。
- **ATT&CK**：TA0008 Lateral Movement（T1021.002 / T1021.001 / T1110）。
- **处置**：①隔离该机（人确认）②重置受影响账号、排查是否拿到域管 ③查 4768/4769 有无异常票据 ④网络分段，445/3389 不跨段。
- **缓解**：办公/服务器/域控分段；普通机无理由 SMB 到全网；4624/4625/4768/4769 全量开；域管不登普通机。

---

## 9.6 本章 checklist

- [ ] 能从 `triage_cluster` 输出里认出"办公机打全网 445"这种高危模式
- [ ] 会用 Zeek conn + Windows 4625/4769 抓横向
- [ ] 能说出 ≥2 个"AI 在横向研判里会翻车"的点
- [ ] 记住：横向失败 ≠ 入侵失败，要拼时间线

下一章：10 · 实战：C2 与外联（INT_10_0_0_7 那段）。
