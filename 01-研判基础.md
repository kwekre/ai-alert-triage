# 01 · 研判基础：告警、事件、误报、漏报

研判前先统一词汇。很多人吵半天，是因为说的不是同一个词。

## 1.1 四个核心概念

| 词 | 英文 | 定义 | 例子 |
|----|------|------|------|
| **告警** | Alert | 规则/模型判定“这里不对劲”的单条信号 | Suricata 命中 `ET SCAN` |
| **事件** | Event | 一条原始记录（日志/流量/端点行为） | `10.0.0.5 访问 /admin 返回 403` |
| **案件** | Incident | 多条告警/事件串成的**真实被入侵或疑似**情形 | “外网 IP 扫描→爆破 SSH→登录成功→内网扫描”整条链 |
| **误报** | False Positive | 告警说有事，实际没事 | 内部扫描器触发“端口扫描”规则 |
| **漏报** | False Negative | 真有事，但没报警 | 0day 绕过所有规则 |

关键关系：**告警是事件的子集提示；案件是告警+事件+上下文拼出来的故事。**
研判的目标，就是把“告警” corroborate 成“是不是案件、多严重、怎么进来的”。

## 1.2 研判要回答的四个问题

每次研判，本质上在填一张表：

```
1) 定性(Qualification): 这到底是真的还是误报？  → 真 / 假 / 待查
2) 定级(Severity):       多严重？               → 参考下表的严重度
3) 归因(Attribution):     谁、用什么手法、从哪进？ → IOC + ATT&CK 技战术
4) 处置(Response):        下一步该干嘛？         → 监控/封禁/隔离/取证/上报
```

### 严重度参考（通用）

| 级别 | 含义 | 典型场景 | 响应时限 |
|------|------|----------|----------|
| Critical | 已在发生的高危入侵 | 域控被拿下、数据库在拖库 | 立即（分钟级） |
| High | 明确攻击尝试且部分成功 | 外网 RCE 利用成功、Webshell 落地 | 1 小时内 |
| Medium | 明确攻击尝试未成功 | 大量 SQLi 探测被 WAF 拦 | 当天 |
| Low | 可疑但弱信号 | 单个奇怪 UA、单次要扫描 | 排期 |
| Info | 仅记录 | 内部合规扫描 | 不处置 |

## 1.3 “判定”的两条铁律

**铁律一：证据链闭合前，不要定性为“真”。**
一条 `SQLi 告警` 单独看是“疑似”。要定性为“真”，最好有：
- 请求里确实含注入 payload（请求证据）
- 响应里确实出现异常（响应证据：报错/数据/延时）
- 最好还有后续动作（如 `UNION` 后开始拖表名）

**铁律二：宁可标“待查”，不要乱标“误报”。**
误报是**确认过没事**才叫误报。没确认就关，是漏报的种子。建议状态机：

```
收到 → 待查(Investigating) → 真(Confirmed) / 假(FalsePositive) / 无法确定(NeedsMoreData)
```

## 1.4 一个最小研判笔记模板（后面每章都用它）

```markdown
## 研判笔记 #<编号>
- 来源告警: <规则名 / 设备 / 时间>
- 资产: <IP/域名/主机名 + 业务重要度>
- 时间窗: <起 ~ 止>
- 初步定性: <真/假/待查>
- 证据:
  - 请求: <...>
  - 响应: <...>
  - 关联: <同源IP的其他告警/流量>
- IOC: <ip / domain / url / hash / user>
- ATT&CK: <战术.技术, 如 TA0001 Initial Access>
- 严重度: <Critical/High/Medium/Low>
- 处置建议: <...>
- 研判人/AI: <谁下的结论 + 置信度>
```

## 1.5 AI 在基础层的定位

AI 不适合替你“拍板定性”（那是责任动作），但极适合：
- 把上面模板**自动填初稿**（从日志抽字段）；
- 提醒你“这条还差什么证据才算闭环”（对照铁律一）；
- 把口语化的“我感觉像扫描”翻译成“建议对照 ET SCAN 类规则 + 看conn.log 速率”。

## 1.6 本章 checklist

- [ ] 能区分 告警 / 事件 / 案件 / 误报 / 漏报
- [ ] 记住研判四问（定性/定级/归因/处置）
- [ ] 记住两条铁律（证据闭合前不定性、不随便标误报）
- [ ] 抄走那份研判笔记模板

下一章：02 · 数据源全景——研判不是只看 SIEM，得知道每块数据“看什么字段”。
