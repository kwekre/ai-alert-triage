# 03 · 流量研判工具链与协议层

流量是很多攻击的“唯一真相源”——端点被关了还有 PCAP。本章给你一套**能直接跑**的命令，并讲清“每层该看什么”。

## 3.1 工具箱（按场景选）

| 工具 | 场景 | 一句话 |
|------|------|--------|
| `tcpdump` | 现场抓包 | 服务器上最常用，轻 |
| `tshark` | 命令行分析 PCAP | Wireshark 的 CLI，过滤极强 |
| Wireshark | 图形深看 | Follow Stream、协议树 |
| **Zeek** | 流量转日志 | 把包变成 `conn/dns/http` 结构化日志 |
| **Brim/Zui** | 看 Zeek 日志 | SQL 式查询 `conn` 日志，爽 |
| Arkime | 全量存+搜 | 流量版 SIEM |
| Mitmproxy | 看明文 HTTP/HTTPS(有CA) | 代理拦包看请求 |

## 3.2 tcpdump：先抓对

```bash
# 抓某 IP 的全部，存文件（研判留证用）
sudo tcpdump -i any -nn -s0 -w capture.pcap host 10.0.0.5

# 只抓 HTTP（端口 80/8080），不存
sudo tcpdump -i any -nn -A -s0 'tcp port 80' | grep -i -E "GET|POST|SQL|union"

# 抓 SYN（看扫描）：只看 TCP 标志 S 且未建立
sudo tcpdump -i any -nn 'tcp[tcpflags] & tcp-syn != 0 and tcp[tcpflags] & tcp-ack == 0'
```

`-s0` 抓全包长度（否则只截头部，看不了 payload）；`-A` 打印 ASCII（明文 HTTP 直接读）。

## 3.3 tshark：命令行主力

```bash
# 列出所有 HTTP 请求：方法 主机 URI 状态码 UA
tshark -r capture.pcap -Y http.request -T fields \
  -e http.request.method -e http.host -e http.request.uri \
  -e http.response.code -e http.user_agent

# 找 SQLi 痕迹：uri 里含 union/select/' 等（大小写不敏感）
tshark -r capture.pcap -Y 'http.request.uri matches "(?i)(union|select|sleep|benchmark|0x)"'

# 找可疑 UA / 空 UA
tshark -r capture.pcap -Y 'http.request and (http.user_agent == "" or http.user_agent matches "(?i)(sqlmap|nikto|nmap|curl|python)")'

# DNS 隧道嫌疑：超长子域名 / 大量 TXT
tshark -r capture.pcap -Y 'dns.qry.type == 16 or dns.qry.name.len > 40' \
  -T fields -e dns.qry.name

# 看某个会话的完整请求响应（Follow）
tshark -r capture.pcap -q -z follow,tcp,ascii,<流号>
```

`-Y` 是显示过滤器（Wireshark 语法），`-T fields -e` 是指定输出列——**研判时几乎永远用这组合**。

## 3.4 协议层：每层看什么

### TCP（看“行为”）
- **SYN 速率高 + 大量 RST/无响应** → 端口扫描。看 `tcp.flags`。
- **重传/乱序多** → 链路或中间人；也常见于隧道塞大包。
- **异常长连接 + 规律小包** → 可能是 beacon（C2 心跳）。

### HTTP（看“意图”）
- `method` 异常：本该 GET 的接口来 POST，或大量 `OPTIONS` → 探测。
- `uri` 含 `../`、`union`、`<script>`、`/actuator`、`/console` → 攻击指纹。
- `status`：`403` 很多 → 在踩权限边界；`500` 连续 → 可能在试注入触发报错。
- `user_agent`：空 / 工具名（sqlmap、awvs）/ 与业务不符 → 高度可疑。

### DNS（看“外联与隧道”）
- 查询名**巨长**、含Base64感字符、高频变化 → DNS 隧道（第 10 章详讲）。
- `TXT` 查询多 → 可能传数据或验证域名。
- 解析到**罕见 TLD / 刚注册域名** → 用威胁情报核对。

### TLS（看“证书与指纹”）
- `JA3` / `JA4`：客户端握手指纹，同一恶意工具 JA3 固定 → 用它在海量流量里**聚同一工具**。
- `SNI` / 证书 CN：是否指向陌生域名；证书年龄短、自签 → 可疑。
- `JA4` 还能区分“浏览器 vs 脚本（python/go）”，对识别自动化很有用。

## 3.5 Zeek：把包变成可查的日志

```bash
# 跑一遍，生成 logs/ 下 conn.log dns.log http.log files.log ...
zeek -r capture.pcap

# 看谁在扫谁（按 src 统计连接数）
cat logs/conn.log | awk '{print $3}' | sort | uniq -c | sort -rn | head

# http.log 里找异常 URI
cat logs/http.log | zeek-cut id.orig_h id.resp_h method uri status_code | grep -iE "union|select|/../"

# dns.log 找长域名
cat logs/dns.log | zeek-cut query | awk '{ if (length($1)>40) print }'
```

`zeek-cut` 按列名取字段，比 awk 下标稳。Brim/Zui 能直接对这些日志写类 SQL。

## 3.6 给 AI 的“喂流量”样例

不要整包丢进去（太大、含敏感 payload）。先抽取结构化：
> 以下是 `http.log` 中抽取的 5 条（已脱敏）：方法/主机/URI/状态/UA。请判断哪些像攻击探测，并给出下一步该查 Zeek 哪个日志。

## 3.7 本章 checklist

- [ ] 会用 tcpdump 抓指定主机的包
- [ ] 会用 tshark `-Y` + `-T fields -e` 抽字段
- [ ] 知道 TCP/HTTP/DNS/TLS 各看什么
- [ ] 会用 Zeek 把 PCAP 转日志

下一章：04 · 告警研判标准动作（SOP）——把“看一眼”变成“可复现七步”。
