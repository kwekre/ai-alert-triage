# 10 · 实战：C2 与外联（INT_10_0_0_7 那段）

Web 被打穿（第 8 章）、内网在横扫（第 9 章），攻击者下一步一定是**回连指挥所（C2）**。C2 流量往往"看起来最正常"——因为它就藏在 HTTPS 里。这一章练"从正常里挑异常"。

---

## 10.0 聚类里的第二个重点

`triage_cluster.py` 输出排第二的是 INT_10_0_0_7（可疑度 11），挂着 `C2信标` + `DNS隧道`。两台失陷机：一台打 Web/横向，一台搞外联——典型的**分工**。

---

## 10.1 时间线：03:05 DNS 隧道，03:10 开始 Beacon

从 `sample_alerts.jsonl` 抽 INT_10_0_0_7：

```
03:05:00  DNS  query=a1b2c3d4...q7r8s9t0u1v2w3x4y5z6.int.corp-internal.test  TXT  长随机子域、高频
03:05:01  DNS  query=z9y8x7w6...int.corp-internal.test                          TXT  同上
03:10:00  TLS  INT_10_0_0_7 -> EXT_1_1_1_1:443   beacon:固定 60s 间隔、时长短、新域名
03:10:00  TLS  INT_10_0_0_7 -> EXT_1_1_1_2:443   beacon:同上
03:10:00  TLS  INT_10_0_0_7 -> EXT_1_1_1_3:443   beacon:同上
```

两段异常：
1. **DNS 隧道**：子域名长得离谱、像 Base64、且高频查 `TXT`——数据被切成小段藏进子域往外挤。
2. **HTTPS Beacon**：连**新域名**、**每 60 秒一次**、**时长短**、目标还换来换去——机器人节奏，不是人。

### 真实命令（有 pcap 时）

```bash
# 长域名
tshark -r dns.pcap -Y 'dns.qry.name.len > 40' -T fields -e dns.qry.name
# TXT 查询偏多
tshark -r dns.pcap -Y 'dns.qry.type == 16' -T fields -e dns.qry.name
# beacon：看 conn.log 的 duration 是否高度一致
zeek-cut id.resp_h duration uid < logs/conn.log | awk '$1 !~ /已知业务IP/' | sort -k2 -n
```

---

## 10.2 JA3 / JA4：用手写"指纹"一把捞同类

TLS 握手里的密码套件/扩展顺序固定成 **JA3（客户端）/ JA4**。同一恶意工具 JA3 恒定——在海量流量里**一把捞出所有同类连接**，比追 IP 快得多。

```bash
tshark -r tls.pcap -Y tls.handshake -T fields -e ip.dst -e tls.handshake.ja3
```

> JA4 还区分浏览器 vs 脚本（python/go），识别"这是个自动化程序连的"更准。**注意**：JA3 可被客户端随机化绕过（见 10.4），所以它是"强线索"不是"铁证"。

---

## 10.3 让 AI 研判——翻车点在这

> **AI 草稿**：INT_10_0_0_7 高频长域名 TXT 查询 + 到新域名的固定间隔 TLS，判定 **True / High C2**；建议阻断域名、隔离主机取证。

> 🔴 **翻车现场 #1（缺基线就下结论）**：AI 把"查了 TXT"直接当隧道。但**单次 TXT 查询很正常**（DKIM 验证、ACME 证书申请都查 TXT）。坐实隧道的是"**长随机子域 + 高频 + 同一父域**"三者叠加。没有基线意识，AI 会把正常 DNS 管 clog 成误报。**人的动作**：先确认"正常 DNS 模式"是什么，再看偏离。

> 🔴 **翻车现场 #2（把 beacon 当心跳）**：AI 见"每 60s 一次 TLS"就标 C2。但监控 agent、云备份客户端也是固定间隔。区别在**目标是不是新域名 + 是否有业务含义**。**人的动作**：beacon 判定 = 新域 + 固定间隔 + 短连接 + 无业务归属，四者齐才稳。

> 🔴 **翻车现场 #3（情报没接）**：判了 High 没说置信度怎么升。把域名/JA3 丢进情报（VirusTotal/OTX/本地 TIP）：若域名新注册或被标恶意、ASN 属已知 C2 托管 → 升 Critical；若无记录但行为异常 → 仍 High，进狩猎队列。

---

## 10.4 红队视角：他怎么藏 C2？

- **域前置（Domain Fronting）**：SNI 写合法 CDN 域名，流量实际被导到 C2——单看 SNI 你看到的是"大厂 CDN"，正常的。得比对 `SNI 声称的域` vs `证书 CN/JA4` vs `真实后端`，不一致即疑（T1090/T1572）。
- **JA3 随机化**：新框架（如飞鱼等）会随机化握手扩展，让 JA3 失效——别只靠 JA3。
- **抖动（Jitter）**：beacon 间隔加随机抖动（如 60±30s），打破"固定间隔"特征。
- **DGA 域名**：用算法生成域名，没有固定 IOC，靠"突然大量查陌生短生命周期域名"识别。

---

## 10.5 处置与缓解

- **定性**：True / High（DNS 隧道 + HTTPS beacon，外联指挥所）。
- **IOC**：父域 `int.corp-internal.test`（长随机子域）；C2 域 `EXT_1_1_1_1/2/3`；JA3（同工具其他连接）。
- **ATT&CK**：TA0011 C2（T1071.004 DNS / T1071.001 HTTPS）。
- **处置**：①阻断该域 + 嫌疑 JA3 ②隔离主机取证、查外泄内容 ③DNS 出口过滤、限制 TXT ④威胁情报核对 ASN。
- **缓解**：DNS 出口白名单 + 审计、TLS 解密（合规内）、JA3/JA4 落地到 SIEM、Egress 最小权限。

---

## 10.6 本章 checklist

- [ ] 能区分"正常 TXT 查询"和"DNS 隧道"（长随机子域+高频+同父域）
- [ ] 知道 beacon = 新域+固定间隔+短连接+无业务归属
- [ ] 会用手势 JA3/JA4 捞同类连接，也知道它可被随机化绕过
- [ ] 识破域前置：SNI 正常 ≠ 流量正常

下一章：11 · 实战：钓鱼与邮件（EXT_2_2_2_2 那封邮件）。
