# 10 · 案例：C2 与外联研判

 attacker 拿下机子后，要“回连”指挥所（C2）。外联流量是发现它的关键——前提是你会看“正常之外”的异常。

## 10.1 先建外联基线

不知道“正常外联什么”，就分不出异常。基线包括：
- 业务常连的域名/IP 段（CDN、API、SaaS）
- 正常 DNS 查询模式（短、低频、有限集合）
- 正常 TLS 指纹（浏览器 JA3/JA4 集合）

异常 = 偏离基线（新域名 + 固定间隔 + 固定指纹）。

## 10.2 DNS 隧道（T1071.004 / T1048）

**特征**：子域名**超长**、含 Base64 感字符（`a-z0-9` 长串）、查询**高频**、多用 `TXT`/`NULL`。数据被切成小段藏进子域。
**查**：
```bash
# 长域名
tshark -r dns.pcap -Y 'dns.qry.name.len > 40' -T fields -e dns.qry.name
# TXT 查询多
tshark -r dns.pcap -Y 'dns.qry.type == 16' -T fields -e dns.qry.name
# 熵/唯一子域计数
tshark -r dns.pcap -T fields -e dns.qry.name | \
  awk -F. '{print $(NF-1)"."$NF}' | sort | uniq -c | sort -rn | head
```
**定性**：长 + 高频 + 随机子域 → **真（High）** 数据外泄/隧道。
**缓解**：DNS 出口过滤、限制 TXT、DNS 审计 + 威胁情报。

## 10.3 ICMP 隧道（T1095 / T1571）

**特征**：ICMP 包 **data 段异常大**、内容像加密/Base64、发往少见主机、间隔规律。
**查**：
```bash
tshark -r icmp.pcap -Y 'icmp.type==8' -T fields -e ip.dst -e data.data | \
  awk '{ if (length($2)>64) print }'
```
**定性**：大 ICMP + 规律 → 疑似隧道（Medium-High）。

## 10.4 HTTPS Beacon（T1071.001）

**特征**：到**新域名**的 TLS 连接，**间隔极规律**（如每 60s）、包大小固定、时长短。
**查（Zeek conn）**：
```bash
zeek-cut id.resp_h duration uid < logs/conn.log | \
  awk '$1 !~ /^(已知业务IP)/' | sort -k2 -n
# 看 duration 是否高度一致（beacon 标志）
```
**定性**：新域 + 固定间隔 + 短连接 → **疑似 C2（High）**。

## 10.5 JA3 / JA4：用握手“指纹”聚同一工具

TLS 握手里的密码套件/扩展顺序固定成 **JA3（客户端）/JA4**。同一恶意工具 JA3 恒定 → 在海量流量里**一把捞出所有同类连接**。
```bash
# Zeek 有 ja3 日志；或 tshark 取 ja3
tshark -r tls.pcap -Y tls.handshake -T fields -e tls.handshake.ja3
```
**价值**：发现“这个 python 脚本连了 50 台内网机”比看 IP 快得多。
**JA4 进阶**：还区分浏览器 vs 脚本（python/go），识别自动化更准。

## 10.6 域前置 / 云 C2（T1090 / T1572）

**特征**：SNI 写的是**合法 CDN 域名**（如大厂 CDN），但实际流量被导向 C2；单看 SNI 会被骗。
**查**：比对“SNI 声称的域” vs “实际证书的 JA4/证书 CN” vs “真实后端”——不一致即疑。
**定性**：SNI/CDN 名正常但证书/行为异常 → **High**，需结合威胁情报与 JA4。

## 10.7 威胁情报核对

把可疑域名/IP/证书/JA3 查情报（VirusTotal、AlienVault OTX、本地 TIP）：
- 域名新注册 / 被标恶意 → 升置信
- ASN 属已知 C2 托管 → 升 Critical
- 无记录但行为异常 → 仍标 High，进狩猎队列

## 10.8 综合研判笔记
```
来源告警: Zeek DNS 长域名 + TXT 高频
资产: INT_10_0_0_5 办公机（不该有外部 DNS 隧道）
定性: 真（High）——疑似 DNS 隧道外泄
IOC: suspect.test（长随机子域）；JA3=abcd...（同工具其他连接）
ATT&CK: TA0011 C2 (T1071.004)
处置: 1)阻断该域 2)隔离主机取证 3)查外泄内容 4)DNS 出口加固
置信度: AI 0.8 + 情报命中
```

## 10.9 本章 checklist

- [ ] 会查 DNS 隧道（长域名/高频/TXT/熵）
- [ ] 会用 JA3/JA4 聚同类连接
- [ ] 知道 beacon = 新域+固定间隔+短连接
- [ ] 识破域前置：SNI 正常 ≠ 流量正常

下一章：11 · 案例：钓鱼与邮件告警研判。
