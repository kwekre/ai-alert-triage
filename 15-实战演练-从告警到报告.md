# 15 · 实战演练：从 28 条告警到一份研判报告

前 14 章把方法讲完了。这一章不教新东西——给你一个真实结构、零真实凭据的脱敏数据集，**你自己走一遍**。所有命令本地能跑，跑完你就有了"我真的研判过一次"的体感。

环境：`python3` 可用即可，无需任何 API/网络。

---

## 15.1 准备

```powershell
cd ai-alert-triage
$env:PYTHONIOENCODING="utf-8"   # Windows 控制台中文不乱码
```

数据集就两份：
- `dataset/sample_alerts.jsonl` —— 28 条脱敏告警（你的"SIEM 导出"）
- `dataset/golden_set.json` —— 15 条已知结论（你的"答案卡"）

---

## 15.2 Step 1 · 聚类排队（5 分钟）

别逐条看。先聚类：

```powershell
python scripts/triage_cluster.py --top 12
```

你要回答一个问题：**这 28 条里，先打哪一台？**

<details>
<summary>参考答案</summary>

排第一的是 `INT_10_0_0_5`（17 条、可疑度 44、挂了 SQLi/SSRF/命令注入/路径遍历/XSS/开放重定向/WebShell/SMB/RDP 九种手法）——单台机同时打 Web 又打内网，这是**一条完整攻击链**，先打它。第二 `INT_10_0_0_7`（C2 信标+DNS 隧道）是外联，第三往后都是单点。

</details>

---

## 15.3 Step 2 · 还原 INT_10_0_0_5 的攻击链（10 分钟）

把它的告警按时间排出来（直接看 `sample_alerts.jsonl` 里 `src=INT_10_0_0_5` 的行）：

```
02:10:11  SMB  -> INT_10_0_1_9:445
02:10:12  SMB  -> INT_10_0_1_10:445
02:10:13  SMB  -> INT_10_0_1_11:445
02:10:30  RDP  -> INT_10_0_0_9:3389  (4625 失败 x3)
02:13:00  GET /item?id=1'                  -> 500 报错
02:13:01  GET /item?id=1+UNION+SELECT...  -> 200 拖出 admin 哈希
02:14:00  GET /search?q=<script>...       -> 200 反射 XSS
02:15:00  GET /fetch?url=169.254.169.254  -> 200 云元数据(SSRF)
02:16:00  GET /p?name=test;echo PRBCMD7Z9 -> 200 命令注入
02:17:00  GET /file?name=../../etc/passwd  -> 200 路径遍历
02:18:00  GET /go?next=https://evil.example.com/ -> 302 开放重定向
02:22:00  POST /upload                     -> 200 WebShell 落地
```

**任务**：给每一行标上"攻击阶段"（踩点 / 边界突破 / 权限维持 / 横向 / 外联）。你会发现 02:10 的 SMB/RDP 其实是**横向**，但此时它还没"进内网"——所以这是**边界机自己在内网扫**，说明它可能已经通过 Web 拿到立足点。这就是第 9 章说的"横向失败 ≠ 入侵失败"。

<details>
<summary>阶段标注参考</summary>

02:10 SMB/RDP = 横向探测/爆破（T1021）；02:13 SQLi = 边界突破+拖库（T1190）；02:14–02:18 = 后续探测（XSS/SSRF/命令注入/遍历/重定向）；02:22 = WebShell 落地（权限维持 T1505.003）。整条链：踩点→突破→探测→维持。

</details>

---

## 15.4 Step 3 · 用黄金集当"阅卷机"（5 分钟）

`golden_set.json` 是 15 条已知结论的"答案卡"。跑评测脚本，看内置规则研判器能拿几分：

```powershell
python scripts/golden_eval.py
```

预期输出：`match_rate = 15/15`，`precision(true) = 1.00`，`recall(true) = 1.00`。

**这步的意义**：前面所有"AI 研判"都是定性的，这一行命令让它变成**可量化的**——这就是第 14 章"研判质量评测"的最小雏形。你以后换更大的模型、加 RAG，都能用同一张黄金集比分数。

**进阶翻车实验**：复制 `golden_set.json` 改一个 alert（比如把 g2 的 `UNION SELECT` 改成 `union select` 小写），再跑——看规则研判器是否还判对。这能让你直观理解"研判器对输入多敏感"，比读十遍理论都管用。

**再进阶**：接本地模型当研判器（需 Ollama/vLLM）：

```powershell
$env:OPENAI_BASE_URL="http://localhost:11434/v1"
$env:OPENAI_MODEL="qwen2.5:32b"
python scripts/golden_eval.py
```

对比规则研判器和 LLM 研判器的 precision/recall，思考"贵在哪、漏在哪"。

---

## 15.5 Step 4 · 出一份研判报告（15 分钟）

挑 INT_10_0_0_5，按下面模板写一份报告。模板就是第 04 章 SOP 的落地：

```markdown
# 研判报告：INT_10_0_0_5 综合入侵事件

## 1. 事件概述
单台办公机 INT_10_0_0_5 在 02:10–02:22 发起 Web 攻击链并横向内网。

## 2. 时间线（按 ts 排序，含请求+响应）
- 02:13:00 GET /item?id=1' -> 500（数据库报错指纹）        → SQLi 确认
- 02:13:01 GET /item?id=1+UNION+SELECT... -> 200 拖出 admin 哈希 → 已读到库
- 02:15:00 GET /fetch?url=169.254.169.254 -> 200 ami-id    → SSRF 打云元数据
- 02:22:00 POST /upload -> 200 落地 shell.php              → WebShell 权限维持

## 3. 受影响资产
INT_10_0_0_5（办公机，异常）、INT_10_0_1_9/10/11（被扫 445）、INT_10_0_0_9（被 RDP 爆破）

## 4. 定性 + 置信
True / Critical（注入已读到库 + 命令执行 + 文件读取 + WebShell 落地）
置信：证据闭环（请求+响应+后续），AI 0.9 / 分析师确认

## 5. IOC
源 INT_10_0_0_5；参数 id/q/url/name/next；
指纹 5f4dcc3b...(示例哈希)、PRBCMD7Z9、mock-instance、shell.php

## 6. ATT&CK
T1190（SQLi/XSS/SSRF/遍历）、T1059.003（命令注入）、T1505.003（WebShell）、T1021.002（SMB 横向）

## 7. 处置建议（需人确认后执行）
① 隔离 INT_10_0_0_5 ② 查是否落马/拿凭证 ③ 相关接口改参数化+关详细报错+WAF
④ 云元数据启用 IMDSv2 ⑤ 网络分段，445/3389 不跨段

## 8. 人确认栏
□ 已确认隔离范围  □ 已确认非靶场流量  □ 已走变更流程
```

---

## 15.6 答案卡（对照 golden_set.json）

| golden id | 对应手法 | 本章/章 |
|-----------|----------|---------|
| g1/g2 | SQLi（报错/联合） | 08 |
| g3 | XSS | 08 |
| g4 | SSRF 云元数据 | 08 |
| g5 | 命令注入 | 08 |
| g6 | 路径遍历 | 08 |
| g7 | 开放重定向 | 08 |
| g8 | WebShell 落地 | 08 |
| g9 | SMB 横向 | 09 |
| g10 | DNS 隧道 | 10 |
| g11 | C2 Beacon | 10 |
| g12 | 钓鱼邮件 | 11 |
| g13/g14 | 良性（登录/资产） | — 误报治理 07 |
| g15 | 单条 DNS 查询（信息不足） | 研判边界 05 |

> 你会发现：**能正确区分 g13/g14（良性）和 g15（信息不足）**，是研判器最关键的能力——乱报 True 是误报，乱报 needs_more_data 是漏报。

---

## 15.7 三个进阶挑战（想练更深）

1. **换研判器比分数**：用 15.4 的 LLM 方式跑 `golden_eval.py`，把规则 vs LLM 的 precision/recall 记下来，写一句"哪种场景 LLM 更强"。
2. **接真实数据**：把你们 SIEM/EDR 的导出，改成 `sample_alerts.jsonl` 同样的字段，直接喂 `triage_cluster.py`。
3. **写一个检测器**：在 `scripts/` 里加一条"办公机打全网 445"的规则（资产白名单 + 445 计数），让它自动给 INT_10_0_0_5 打 High——这就是第 12 章工作流的"采集→归一化→判定"落地。

---

走完这四步，你就不是"读过研判"，而是"研判过一次"。回到开头那句话：**AI 给建议，人负最终责任**——报告第 8 节那三个勾，得是你亲自打的。

仓库：https://github.com/kwekre/ai-alert-triage
