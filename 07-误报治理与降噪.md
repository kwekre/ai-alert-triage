# 07 · 误报治理与降噪

SOC 崩溃的主因不是攻击多，是**误报多**。治理好误报，AI 才能把人力留给真告警。

## 7.1 误报从哪来

| 来源 | 例子 |
|------|------|
| 规则太宽 | `ET SCAN` 把内网正常探测也报警 |
| 内部扫描器/资产发现 | 运维 Nmap、CMDB 探测触发“端口扫描” |
| 业务脚本/健康巡检 | 心跳请求被当“目录爆破”（大量 404） |
| 测试/预发流量 | 压测、混沌工程产生“异常” |
| 白名单缺失 | 已知 CDN/合作伙伴 IP 没放行 |

## 7.2 四类治理手段（从根上减）

**① 资产/业务白名单（最快见效）**
把已知扫描器、CMDB、CDN 边缘 IP 放进 `SUPPRESS` 列表，从源头不报警。
Suricata 用 `suppress` 或 `threshold`；SIEM 用 lookup 表。

```yaml
# Suricata threshold.conf 示例：放行内部扫描器
suppress gen_id 1, sig_id 2000001, track by_src, ip 10.0.0.99/32
```

**② 阈值/次数调优**
“1 次 404”不报，“1 分钟内 200 次 404”才报。用 `threshold` 的 `type limit/threshold/both`。

**③ 关联抑制（告警聚类）**
同一源 IP 5 分钟内 50 条 `ET SCAN` → 聚成 1 条“扫描事件”，严重度取最高。

**④ 基线学习**
对 Web 的“正常 UA/路径/速率”建基线，偏离才报。简单做法：统计近 7 天分布，超出 N 倍标准差标异常。

## 7.3 用 AI 做“初筛降噪”

流程：海量告警 → AI 聚类 + 标 `suspicion(0-1)` → 人只审 top-K。

提示词（沿用第 05/06 模板）：
```
给定 500 条告警（JSON 数组，已脱敏），请：
1) 聚成不超过 20 起事件（按 src_ip/手法）；
2) 每起给 suspicion 与一句话理由；
3) 只输出 suspicion>=0.7 的，按降序。
不编造字段，缺失标 unknown。
```
人从 500 条降到审 ~20 起，精力用在刀刃上。

## 7.4 一个真实治理案例

**现象**：每天 8000 条 `ET SCAN`，分析师全忽略 → 真扫描也被淹没。
**根因**：`HOME_NET` 配错，内网互访算“外部扫描”；运维 Nmap 没放行。
**治理**：
1. 修正 Suricata `HOME_NET` 为真实内网段；
2. 把 CMDB/Nmap 源 IP 加 `suppress`；
3. 对扫描类加 `threshold`（1 分钟 > 100 连接才报）；
4. 用 AI 对剩余告警做日聚类，输出 top 事件。
**结果**：日均告警 8000 → 120，真扫描可见。

## 7.5 闭环：把“人确认”回灌

分析师标“误报”的，沉淀成规则：要么加白名单，要么调阈值。**误报不回灌 = 永远手标**。
建议建一张 `false_positive_feedback` 表，周维度复盘，驱动规则迭代。

## 7.6 指标（治理好坏看这个）

| 指标 | 含义 | 目标方向 |
|------|------|----------|
| 日均告警量 | 噪声规模 | 降 |
| 误报率（FP%） | 标误报占比 | 降 |
| 分析师处理单条耗时 | 效率 | 降 |
| 真告警平均检出时延 | 漏不漏 | 降 |

## 7.7 本章 checklist

- [ ] 能说出 4 类误报来源
- [ ] 会用白名单/阈值/聚类/基线四手段
- [ ] 会用 AI 对海量告警做 top-K 初筛
- [ ] 建立“误报回灌”闭环

下一章：08 · 案例：Web 攻击流量研判（动手看真流量）。
