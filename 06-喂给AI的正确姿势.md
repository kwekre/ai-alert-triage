# 06 · 喂给 AI 的正确姿势

直接把原始日志贴进 AI = 三输：泄密、截断、被噪声带偏。本章给可落地的“预处理三板斧”。

## 6.1 为什么不能直接贴原始数据

| 问题 | 后果 |
|------|------|
| 含 PII / 凭证 / 内部 IP | 合规事故、数据出域 |
| 文件太大（PCAP 几 MB） | 超上下文被截断，AI 只看前半段 |
| 字段杂乱（全量 conn.log） | AI 被无关列干扰，结论发散 |

正确顺序：**先抽字段 → 再脱敏 → 再分块 → 最后带模板喂**。

## 6.2 第一板斧：抽字段（别整包丢）

把 PCAP/日志先转成窄表，用第 03 章命令：

```bash
# http.log → 只留研判要的 5 列，存 csv 给 AI
zeek-cut id.orig_h id.resp_h method uri status_code < logs/http.log \
  > http_narrow.csv

# 大文件按时间窗切片，避免超长
tshark -r big.pcap -Y "http.request" -T fields \
  -e frame.time -e ip.src -e http.request.uri | head -200
```

给 AI 的应是**窄表 + 行数可控（建议 < 200 行/次）**。

## 6.3 第二板斧：脱敏（上线前必做）

替换规则（保留结构、去掉真相）：

| 原值 | 替换为 | 目的 |
|------|--------|------|
| 内部 IP `10.x/192.168.x/172.16-31.x` | `INT_10_0_0_5` 等占位 | 去真实拓扑 |
| 公网 IP | `EXT_1_2_3_4` | 去可回溯身份 |
| 真实域名 | `example.com` / `suspect.test` | 去业务信息 |
| 邮箱/手机号/姓名 | `<redacted>` | 去 PII |
| 密码/Token | 删除整行 | 去凭证 |
| 文件哈希 | 保留前 8 位 `ab12cd34*` | 可查重不可还原 |

> 原则：**AI 研判靠“模式”，不靠“真实值”**。模式（长域名、异常 UA、频率）在脱敏后依然在。

## 6.4 第三板斧：分块 + 给上下文包

一次只喂一个“研判单元”，并附**范围声明**：

```
【我有这些数据源】Zeek conn/dns/http、Suricata 告警、EDR 进程树。
【资产】INT_10_0_1_9 是对外 Web 服务器，有 WAF。
【时间窗】02:00-02:30。
【以下为脱敏后的 http_narrow.csv（120 行）】
<csv>
请：标记疑似攻击行，给 IOC 与下一步查哪个日志。
```

缺“范围声明”，AI 会瞎假设你有它其实没有的数据。

## 6.5 输出约束（固定模板）

沿用第 05 章 JSON 模板，并加一条硬约束：
> **只引用我提供文本中的原文；凡证据不足，verdict 必须为 needs_more_data；不得编造 status/响应内容。**

## 6.6 脱敏前后对比

脱敏前（危险）：
```
10.0.0.5 - - [02:13] "POST /login pwd=Admin@123&user=root" 200
```
脱敏后（可喂）：
```
INT_10_0_0_5 - - [02:13] "POST /login <creds-redacted>" 200
```
模式（爆破式登录、200 成功）保留，真相消失。

## 6.7 自动化脱敏小脚本（思路）

用正则批量替换再入库/喂模型：
```python
import re
def mask(text):
    text = re.sub(r'\b10\.\d{1,3}(?:\.\d{1,3}){2}\b', 'INT_10_x', text)
    text = re.sub(r'pwd=[^&\s]+', 'pwd=<redacted>', text)
    return text
```
（实战用现成 DLP/脱敏工具更稳，这里只演示思路。）

## 6.8 本章 checklist

- [ ] 先抽字段再喂，单包 < 200 行
- [ ] 内部/公网 IP、域名、PII、凭证都脱敏
- [ ] 每次都带“数据源范围 + 资产 + 时间窗”
- [ ] 输出强制 JSON + “只引用原文”约束

下一章：07 · 误报治理与降噪——让 AI 先替你筛掉 99% 噪声。
