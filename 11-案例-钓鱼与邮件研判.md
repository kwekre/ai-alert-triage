# 11 · 实战：钓鱼与邮件（EXT_2_2_2_2 那封邮件）

钓鱼是初始访问（TA0001）最高频的入口。前面三章是"进来之后"，这一章是"怎么进来的"。邮件研判看四层：**头、URL、附件、话术**。

---

## 11.1 这封邮件长这样（脱敏自 `sample_alerts.jsonl`）

```
09:30:00  模块=mail
From:        "财务通知" <fin-notice@okpay-test.top>      # 陌生域 + typo 感
To:          all-staff@company.com
Subject:     【紧急】Q3 薪资明细，今日截止确认
Authentication-Results: spf=fail  dkim=fail  dmarc=fail   # 三项全挂
Attachment:  payroll_q3.docm
宏(oleVBA):  Sub AutoOpen -> IEX (New-Object Net.WebClient).DownloadString(...)
```

四层一起看：
1. **头**：发件域 `okpay-test.top` 陌生，且 **SPF/DKIM/DMARC 全 fail** → 高度伪造。
2. **显示名仿冒**：`"财务通知"` 看着像内部财务，但真实地址是 `fin-notice@okpay-test.top`——**看 `From:` 真实地址，别看显示名**。
3. **附件**：`.docm` 带宏，`AutoOpen` 自动执行 `IEX(DownloadString(...))` → 一打开就拉恶意脚本（T1566.001）。
4. **话术**："紧急""Q3 薪资""今日截止" → 制造紧迫感逼你别想直接点。

---

## 11.2 真实命令：不点开也能查

```bash
# 抽认证结果 + 真实发件
grep -E "Authentication-Results|From:|Return-Path" suspicious.eml

# 附件真实类型（伪装 .pdf.exe 现形）
file attachment.bin
sha256sum attachment.bin

# 抽 VBA 宏（有 IEX/PowerShell 即高危）
olevba attachment.docm
```

> ⚠️ **永远别在自己浏览器/电脑直接打开可疑附件或点链接**。用沙箱（任意 detonation 沙箱）或 VirusTotal/OTX 查最终落地。这一步是"人"的纪律，AI 替代不了。

---

## 11.3 让 AI 研判——翻车点

> **AI 草稿**：该邮件发件域陌生、三项认证失败、宏自动下载执行、话术紧急，判定 **True / High 钓鱼**；建议隔离收件人、阻断域名、全员提醒。

> 🔴 **翻车现场 #1（被显示名骗）**：若你只贴了 `From: 财务通知`，AI 可能顺着写"疑似财务部门通知"。**必须贴真实 `From:` 地址**，显示名不可信。这是"喂数据的方式"决定了结论（回扣第 06 章）。

> 🔴 **翻车现场 #2（把附件当文档）**：AI 见 `payroll_q3.docm` 可能当成"薪资文档"描述。必须让它看 `oleVBA` 输出里的 `IEX/DownloadString`——那才是定性关键。**人的动作**：研判附件看"行为"（宏做了什么），不是"文件名"。

> 🔴 **翻车现场 #3（建议"先打开确认"）**：极少数模型会建议"打开看看是不是真的"。**绝对不行**——打开即失陷。正确建议是"沙箱分析 + 隔离收件人 + 阻断域"，打开是红线。

---

## 11.4 红队视角：他怎么提高钓鱼成功率？

- **显示名仿冒高管**：`CEO <ce0@evil.test>` 显示成 `CEO`，员工只看名字就信。
- **_reply-chain 劫持_：潜伏后回复真实邮件线程，"顺手"加个恶意链接，上下文极真。
- **宏 + LNK + ISO**：绕过多数"禁 exe"策略（.docm/.lnk/.iso 不在黑名单里）。
- **同形域名（typosquat）**：`cornpany.com`、`arnazon.com` 肉眼难辨。
- **紧急话术**：账号冻结、薪资、税单、发票——都是"让你来不及想"的主题。

---

## 11.5 处置与缓解

- **定性**：True / High（初始访问载体，宏自动下载执行）。
- **IOC**：发件域 `okpay-test.top`；附件哈希；宏特征 `IEX/DownloadString`；主题关键词。
- **ATT&CK**：T1566.001 钓鱼附件。
- **处置**：①隔离收件人邮箱、阻断发件域 ②沙箱取证附件 ③全员一键上报 + 提醒 ④查是否已有其他收件人中招。
- **缓解**：DMARC `p=reject` + 严格 SPF/DKIM；附件沙箱 + 禁宏策略；显示名保护；用户培训 + 上报按钮。

---

## 11.6 本章 checklist

- [ ] 会读 SPF/DKIM/DMARC 判定伪造（三项 fail = 高危）
- [ ] 识破显示名仿冒（永远看真实 `From:`）
- [ ] 会用 `olevba`/`file` 查附件真实类型与宏行为
- [ ] 牢记：可疑附件/链接**只在沙箱查**，绝不本机打开

回到主线：12 · 构建 AI 研判工作流（把 08–11 这些方法串成工程）。
