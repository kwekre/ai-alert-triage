# 11 · 案例：钓鱼与邮件告警研判

钓鱼是初始访问（TA0001）最高频入口。研判邮件，看**头、URL、附件、话术**四层。

## 11.1 邮件头：发件人到底是谁

**SPF/DKIM/DMARC**：三项任一失败 → 高度可疑（伪造）。
```
Authentication-Results: spf=fail (sender IP not authorized)
                     dkim=fail (signature mismatch)
                     dmarc=fail (policy)
```
**显示名仿冒**：`ceo@company.com` 真，但显示名写 `CEO <ce0@evil.test>`——看 `From:` 实际地址，别看显示名。
**路由异常**：`Received:` 链出现陌生中继、境外跳板。

**查（网关/邮件日志）**：
```bash
# 提取一封信的认证结果
grep -E "Authentication-Results|From:|Return-Path" suspicious.eml
```

## 11.2 恶意 URL

**特征**：短链（bit.ly）、同形域名（`arnazon.com`/`cornpany.com`）、多级重定向、紧迫路径（`/invoice/verify`）。
**研判（脱敏后）**：
- 用沙箱/VirusTotal 查最终落地（**别在自己浏览器直接点**）
- 看域名注册年龄（新注册 = 高危）
- 看是否匹配已知品牌 typo
**给 AI**：贴“显示 URL + 重定向链（已沙箱解析）”，让它判可疑度 + 下一步。

## 11.3 恶意附件

**高危类型**：`.exe/.scr/.bat`、`.docm/.xlsm`（含宏）、`.iso/.img`、`.lnk`、`.hta`、带宏的 PDF。
**查**：
```bash
# 提取附件哈希
sha256sum attachment.bin
# 查情报
# (VirusTotal / OTX / 本地 TIP)
file attachment.bin            # 看真实类型（伪装 .pdf.exe）
olevba attachment.docm         # 抽 VBA 宏（有 IEX/PowerShell 即高危）
```
**定性**：宏调 PowerShell/下载执行 → **真（High）** 初始访问载体。

## 11.4 社会工程话术

红队常用：紧急（“账号将在 1 小时冻结”）、机密（“薪资调整，勿外传”）、冒充高管（CEO 诈骗）、伪装 IT（“重置 VPN”）。
**判**：话术 + 异常发件 + 异常链接 → 串起来才下结论。

## 11.5 实战：一封可疑邮件头研判
```
From: "财务通知" <fin-notice@okpay-test.top>   # 陌生域 + typo 感
To: all-staff@company.com
Subject: 【紧急】Q3 薪资明细，今日截止确认
Authentication-Results: spf=fail dkim=none dmarc=fail
Attachment: payroll_q3.docm  (olevba: Sub AutoOpen -> IEX (New-Object Net.WebClient).DownloadString)
```
AI 应给：①发件域陌生+认证全失败 ②宏自动下载执行 ③话术紧急诱导 → **真（High）钓鱼**，建议隔离收件人+阻断域+全员提醒。
**动作仍须人确认**（第 05 章）。

## 11.6 缓解要点

- 强制 **DMARC p=reject** + 严格 SPF/DKIM
- 附件沙箱 + 宏禁用策略
- 显示名保护（防仿冒高管）
- 用户培训 + 一键上报按钮

## 11.7 本章 checklist

- [ ] 会读 SPF/DKIM/DMARC 判定伪造
- [ ] 识破显示名仿冒（看真实 From）
- [ ] 会用 olevba/file 查附件真实类型与宏
- [ ] 不点可疑链接，用沙箱/情报查

下一章：12 · 构建 AI 研判工作流（把前面串成工程）。
