# 13 · 私有化与本地部署

把真实日志喂公网模型 = 合规红线。本章讲**数据不出域**怎么落地，以及模型怎么选。

## 13.1 为什么私有化

- **合规**：等保/GDPR/客户合同常要求日志不出境、不出域。
- **可控**：模型版本、提示词、知识库都在自己手里。
- **成本可预期**：API 按量计费，量大时本地 GPU 更划算。
- **离线**：断网环境（关基/涉密）必须本地。

## 13.2 方案选型

| 场景 | 方案 | 说明 |
|------|------|------|
| 个人/小队 | **Ollama** | 一行 `ollama run qwen2.5` 起本地模型 |
| 图形/易用 | **LM Studio** | 界面化管理本地模型 |
| 团队/并发 | **vLLM / Ollama 服务化** | 起 OpenAI 兼容 API，多人共用 |
| 带界面 RAG | **Open WebUI** | 接 Ollama/vLLM，有聊天+知识库 |
| 接 Agent | **OpenClaw / Cline / Continue / Aider** | 配置 `OPENAI_BASE_URL` 指向本地 |

> 接 Agent 的三环境变量（与 ai-pentest-guide 第 12 章一致）：
> `OPENAI_API_KEY`（随便填）/ `OPENAI_BASE_URL=http://localhost:11434/v1` / `OPENAI_MODEL=qwen2.5:32b`

## 13.3 模型选型（研判任务够用就行）

研判是**长上下文 + 归纳 + 抽取**，不一定需要最大模型：

| 定位 | 国外 | 国内（推荐，延迟低/合规好） |
|------|------|------------------------------|
| 强 | GPT-4o / Claude | Qwen2.5-72B / DeepSeek-V3 |
| 均衡（常用） | — | **Qwen2.5-32B / DeepSeek-R1-32B** |
| 轻量本地 | Llama3-8B | Qwen2.5-7B/14B |

经验：**32B 级** 跑研判草稿+聚类+抽取已很稳；需要复杂推理（串多步攻击链）再上 70B+。

## 13.4 本地知识库（RAG 不出域）

- 向量库（Chroma / Qdrant / 本地 FAISS）存 RUNBOOK、资产、历史案件、内部情报。
- 文档放本地（如 IMA 知识库 / 内网 Wiki），检索只在本地。
- 让 AI 引用本地文档，结论可审计、不外泄。

## 13.5 成本与硬件

| 模型 | 显存（量化后） | 备注 |
|------|----------------|------|
| 7B (Q4) | ~5 GB | 笔记本可跑 |
| 14B (Q4) | ~9 GB | 消费显卡 |
| 32B (Q4) | ~18-20 GB | 单张 24G 卡 |
| 70B (Q4) | ~40 GB | 双卡/服务器 |

本地一次投入硬件，长期比 API 便宜；但要运维。小队先用 Ollama + 32B 起步性价比最高。

## 13.6 评估：本地模型够不够

别盲信。用第 14 章的**黄金集**测：
- 给 50 条已知结论的告警，看本地模型 verdict 命中率。
- 低于可接受阈值 → 换更大模型或加 RAG/示例（few-shot）。

## 13.7 与第 12 章工作流结合

私有化部署后，第 12 章架构的 `llm.judge` 指向本地 `OPENAI_BASE_URL`：
```
采集→归一化→RAG(本地)→llm.judge(本地模型)→人确认→SOAR
```
全链路不出域，合规与效果兼得。

## 13.8 本章 checklist

- [ ] 知道 Ollama/LM Studio/vLLM/OpenWebUI 各自定位
- [ ] 会用三环境变量把 Agent 指向本地模型
- [ ] 研判任务 32B 级通常够用
- [ ] 本地 RAG 知识库可审计不出域
- [ ] 用黄金集评估本地模型质量

下一章：14 · 指标与持续改进（收尾）。
