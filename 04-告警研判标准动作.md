# 04 · 告警研判标准动作（SOP）

把“看一眼”变成“可复现、可交接、可审计”的流程。无论 AI 与否，这套七步法是骨架。

## 4.1 七步研判清单

```
① 接警    看规则名/源IP/目的/时间/资产重要度
② 资产    这资产干嘛的？重要吗？谁能碰？
③ 上下文  同源IP/同会话/同时间窗的其他信号
④ 提 IOC   ip / domain / url / hash / user / file
⑤ 时间线  按时间把事件排成一条因果链
⑥ 映射     对 ATT&CK：战术.技术
⑦ 结论     定性/定级/处置 + 置信度 + 谁拍的板
```

下面逐步展开，并标出**哪步最适合让 AI 帮忙**。

## 4.2 ① 接警：先读“元数据”，别急着点 payload

拿到告警先看字段，不要一上来翻包：
- **规则名**：`ET SCAN` vs `ET TROJAN` vs `Web SQLi` → 完全不同方向。
- **源/目的 IP + 端口**：外网→内网？内网→内网？服务端端口？
- **时间**：工作时间还是凌晨？一次性还是持续？
- **资产重要度**：这是办公机还是域控/数据库？严重度直接挂钩。

> AI 可帮：把告警 JSON 自动压成“一句话摘要 + 关键字段表”，省你读原始结构的时间。

## 4.3 ② 资产盘点：决定要不要现在慌

问三件事：
1. 资产角色（Web/DB/域控/员工PC/跳板）？
2. 暴露面（公网还是内网？有无 WAF？）？
3. 谁有权访问（正常该有哪些流量）？

如果是**域控被外网直连**，直接升 Critical；如果是**测试机被内网扫描**，先 Medium。

## 4.4 ③ 上下文：单条告警几乎没意义，要拉“同一主体”

关键动作——以 **源 IP / 目的 IP / 会话 ID** 为轴，把时间窗±1~24h 的所有信号拉出来：
- 同一源 IP 之前/之后还有什么告警？（扫描→爆破→利用 往往是串的）
- 同一会话（flow_id / stream）的整个请求响应链？
- 目的主机同时段还有没有别的异常？

```bash
# Suricata：查某源 IP 的全部告警签名
jq -r 'select(.src_ip=="10.0.0.5") | .alert.signature' eve.json | sort | uniq -c

# Zeek conn：该源 IP 连了哪些目的
grep "10.0.0.5" logs/conn.log | zeek-cut id.resp_h id.resp_p | sort -u
```

> AI 可帮：给定一组告警，自动**聚类成几起事件**，并画“时间-主体”草图。

## 4.5 ④ 提 IOC：把“看到的”变成“可追的”

从请求/响应/进程里抽：
- IP（内外）、域名、URL、文件哈希、邮箱、用户名、注册表路径。
统一成 STIX-lite 列表，方便后面查情报/写阻断。

## 4.6 ⑤ 时间线：还原因果，不是堆时间

按时间排序后，要能讲出故事：
```
02:11  外网 1.2.3.4 对 Web 做目录爆破（大量 404）
02:13  出现 /admin 403 → 试越权
02:15  POST /login 多次 401 → 爆破
02:17  登录成功 200 → 疑似拿到账号
02:19  该账号访问 /api/export 下载大量数据
```
这一步最该人做（需要领域知识判断“这算成功吗”）；AI 可帮你**把零散日志自动排成时间线草稿**。

## 4.7 ⑥ 映射 ATT&CK：给结论一个“行话坐标”

| 现象 | ATT&CK |
|------|--------|
| 端口/目录扫描 | TA0043 Reconnaissance / T1046 |
| 爆破登录 | TA0006 Credential Access / T1110 |
| SQLi 拖库 | TA0009 Collection / T1213 |
| 内网 SMB/PTH | TA0008 Lateral Movement / T1021/T1550 |
| 外联 C2 | TA0011 Command & Control / T1071/T1572 |

映射后，报告别人秒懂，也方便和红队/合规对齐。

## 4.8 ⑦ 结论：定性 + 定级 + 处置 + 置信度

回到第 01 章模板：
```
初步定性: 真（证据闭合：请求含 payload + 响应 200 + 后续下载）
严重度:   High
归因:     外网爆破登录 → 数据导出（ATT&CK T1110→T1213）
处置:     1) 冻结该账号 2) 阻断源IP 3) 复查导出数据落点 4) 加固 /admin 鉴权
置信度:   AI 0.8 / 分析师确认
```

**铁律回扣**：证据没闭合 → 标“待查”，不标“误报”。

## 4.9 一个可复制的研判笔记（直接抄）

见 01 章模板。每次都按它写，交接、复盘、喂 AI 都方便。

## 4.10 本章 checklist

- [ ] 能默念七步（接警→资产→上下文→IOC→时间线→映射→结论）
- [ ] 会用 jq/zeek-cut 按主体拉上下文
- [ ] 会把一条攻击映射到 ATT&CK
- [ ] 结论里永远带“置信度 + 谁拍板”

下一章：05 · AI 能做什么、不能做什么——避免把 AI 当算命。
